# Termux commands

- `setup.sh` installs prerequisites.
- `run-audit.sh` validates, builds, starts the local server, and opens the audit URL.
- `new-audit-note.sh` creates a timestamped Markdown audit template.
- `stop.sh` stops the local server.
