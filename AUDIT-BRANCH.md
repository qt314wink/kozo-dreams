# Kozo Dreams v3.2 visual-audit branch

This branch adds the reproducible Termux build, serve, stop, validation, and audit-note workflow used for the v3.2 visual review.

The complete v3.2 source bundle was validated locally with:

```bash
npm run check
npm run build
npm run serve
```

Review the interaction journey, hierarchy, typography, mobile behavior, motion, reduced-motion state, and completion confidence before promotion.
